# Generated by Conveyor — do not edit.
# Regenerated from the project's service dependencies on every build; local
# edits are overwritten the next time the generated content changes.
services:
  agent:
    image: "ghcr.io/fitzzero/quickdraw-chat/conveyor/agent:current"
    command:
      - "sleep"
      - "infinity"
    volumes:
      - "../../..:/workspaces:cached"
