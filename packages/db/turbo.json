{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "db:generate"],
      "inputs": [
        "src/**",
        "package.json",
        "tsconfig.json",
        "tsup.config.ts",
        "prisma/schema.prisma",
        "prisma/generated/**"
      ],
      "outputs": ["dist/**"]
    }
  }
}
