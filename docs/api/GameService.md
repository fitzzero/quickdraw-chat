# GameService

**Service Name:** `gameService`

**Access Control:** Custom (self-access pattern)

---

## Methods

### joinGame

- **Access Level:** `Read`
- **Socket Event:** `gameService:joinGame`
- **Validation:** Zod schema (`worldScopedSchema`)

**Payload Schema:**

```typescript
{
  worldId: z.string().min(1),
}
```

### watchWorld

- **Access Level:** `Public`
- **Socket Event:** `gameService:watchWorld`
- **Validation:** Zod schema (`worldScopedSchema`)

**Payload Schema:**

```typescript
{
  worldId: z.string().min(1),
}
```

### respawn

- **Access Level:** `Read`
- **Socket Event:** `gameService:respawn`
- **Validation:** Zod schema (`worldScopedSchema`)

**Payload Schema:**

```typescript
{
  worldId: z.string().min(1),
}
```

### leaveGame

- **Access Level:** `Read`
- **Socket Event:** `gameService:leaveGame`
- **Validation:** Zod schema (`worldScopedSchema`)

**Payload Schema:**

```typescript
{
  worldId: z.string().min(1),
}
```

### getWorld

- **Access Level:** `Public`
- **Socket Event:** `gameService:getWorld`
- **Validation:** Zod schema (`getWorldSchema`)

**Payload Schema:**

```typescript
{
  slug: z.string().min(1).max(64),
}
```

### getMyBest

- **Access Level:** `Read`
- **Socket Event:** `gameService:getMyBest`
- **Validation:** Zod schema (`worldScopedSchema`)

**Payload Schema:**

```typescript
{
  worldId: z.string().min(1),
}
```

### getHighScores

- **Access Level:** `Public`
- **Socket Event:** `gameService:getHighScores`
- **Validation:** Zod schema (`highScoresSchema`)

**Payload Schema:**

```typescript
{
  worldId: z.string().min(1),
  limit: z.number().int().min(1).max(100).optional(),
}
```

---

## Access Levels

| Level    | Description                      |
| -------- | -------------------------------- |
| Public   | No authentication required       |
| Read     | Authenticated users can read     |
| Moderate | Can edit content, manage members |
| Admin    | Full control, can delete         |

---

_Generated by `scripts/generate-docs.ts`_
